import { COLORS } from './theme';

export default {
  light: {
    text: COLORS.textPri,
    background: COLORS.bg,
    tint: COLORS.teal,
    tabIconDefault: COLORS.textSec,
    tabIconSelected: COLORS.teal,
  },
  dark: {
    text: COLORS.textPri,
    background: COLORS.bg,
    tint: COLORS.teal,
    tabIconDefault: COLORS.textSec,
    tabIconSelected: COLORS.teal,
  },
};
